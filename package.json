{
  "name": "wordpress-plugin-image-watermark",
  "version": "0.1.0",
  "scripts": {
    "wp-env": "wp-env",
    "clean": "rimraf build",
    "start": "npm run clean && wp-scripts start",
    "build": "npm run clean && wp-scripts build",
    "postinstall": "composer install"
  },
  "devDependencies": {
    "@wordpress/env": "^5.9.0",
    "@wordpress/scripts": "^25.3.0",
    "rimraf": "^4.1.2"
  },
  "dependencies": {
    "@wordpress/api-fetch": "^6.24.0",
    "@wordpress/components": "^23.3.0",
    "@wordpress/core-data": "^6.3.0",
    "@wordpress/data": "^8.3.0",
    "@wordpress/element": "^5.3.0",
    "@wordpress/media-utils": "^4.17.0"
  }
}
